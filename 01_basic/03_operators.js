//In programing the operators are used to perform operations on given variables or value or data.
//They together with variables which are called operands, form the basic building blocks of a program known as exression.
//There are different types of operators in JavaScript, such as arithmetic operators, comparison operators, logical operators, assignment operators, and more.

//1. Arithmetic Operators: These operators are used to perform arithmetic operations on numbers. The common arithmetic operators are:
// In this basic arithmetic operators are + (addition), - (subtraction), * (multiplication), / (division), % (modulus), ++ (increment), and -- (decrement).

let a = 20;
let b = 45;

// console.log(a + b); //addition:65
// console.log(b - a); //subtraction:25
// console.log(a * b); //multiplication:900
// console.log(b / a); //division:2.25
// console.log(b % a); //modulus:5
// console.log(a++); //increment:20
// console.log(++a); // increment:22

//2. Comparison Operators: These operators are used to compare two values and return a boolean value (true or false). The common comparison operators are:
// == (equal to), === (strict equal to), != (not equal to), !== (strict not equal to), > (greater than), < (less than), >= (greater than or equal to), <= (less than or equal to).

let x = 10;
let y = "10";

// console.log(x == y); //true (equal to)
// console.log(x === y); //false (strict equal to)
// console.log(x != y); //false (not equal to)
// console.log(x !== y); //true (strict not equal to)
// console.log(x > y); //false (greater than)
// console.log(x < y); //false (less than)

//3. Logical Operators: These operators are used to combine multiple conditions and return a boolean value (true or false). The common logical operators are:
// && (logical AND), || (logical OR), ! (logical NOT).

let p = true;
let q = false;

console.log(p && q); //false (logical AND)
console.log(p || q); //true (logical OR)
console.log(!p); //false (logical NOT)
